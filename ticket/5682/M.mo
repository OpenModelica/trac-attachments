model M
  Modelica.Blocks.Sources.Constant const(k = 1)  annotation(
    Placement(visible = true, transformation(origin = {48, 24}, exxtent = {{-20, -20}, {20, 20}}, rotation = 0)));
equation

annotation(
    uses(Modelica(version = "3.2.3")));
end M;
